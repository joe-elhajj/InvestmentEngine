requests>=2.31
PyYAML>=6.0
pytest>=7.0
# HTML-to-text for engine/filings.py (10-K document parsing, Tier 2). Was
# already present transitively via yfinance; declared directly now that
# engine code imports it.
beautifulsoup4>=4.12
# Optional: live price/market-cap source. Free but fragile; swap for a paid
# provider (FMP, EODHD, Polygon) later behind engine/market.py.
yfinance>=0.2.40
# Local web layer (app/) — wraps the engine, no engine code depends on these.
fastapi>=0.111
uvicorn[standard]>=0.29
# httpx: not an app runtime dependency, but required by fastapi.testclient
# (Starlette's TestClient is httpx-based) — needed to run tests/test_app.py.
httpx>=0.27
# Tier 2 (engine/flags.py) — the ONLY LLM call in the codebase. Selects
# verbatim spans from 10-K text; every returned snippet is validated as an
# exact substring before it's trusted (see engine/flags.py's verify_verbatim).
anthropic>=0.40
